# opencv-python-headless MUST be listed BEFORE rapidocr_onnxruntime
# so pip installs the headless (no X11/libxcb) version first.
# rapidocr will then reuse it instead of pulling full opencv.
opencv-python-headless>=4.8.0.76
rapidocr_onnxruntime>=1.3.0
pyTelegramBotAPI>=4.12.0
Pillow>=10.0.0
python-dotenv>=1.0.0
zxing-cpp>=2.2.0
numpy>=1.24.0
